{TESTE OK}
programa teste_proced;

var x,y: inteiro;

procedimento imprime;
inicio
    escreva(x)
fim;

procedimento soma;
var a,b: inteiro;
inicio
    leia(a);
    leia(b);
    x := a + b
fim;

inicio
    soma;
    imprime
fim.
